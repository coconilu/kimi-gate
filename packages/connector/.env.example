# kimi-gate connector 配置示例 —— 复制为 .env 并填入真实值（.env 不提交 git）

# Gateway 的 WSS 地址（域名指向你的 VPS）
GATEWAY_URL=wss://gate.example.com

# 配对密钥：gateway 服务器上 `npm run setup` 输出的 CONNECTOR_KEY
CONNECTOR_KEY=

# 本地 kimi web 地址（默认即可）
KIMI_LOCAL_URL=http://127.0.0.1:58627
